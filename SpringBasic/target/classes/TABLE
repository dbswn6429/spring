CREATE TABLE SCORE (
    SNO NUMBER,
    NAME VARCHAR2(30),
    KOR VARCHAR2(30),
    ENG VARCHAR2(30),
    MATH VARCHAR2(30)
);

ALTER TABLE SCORE ADD CONSTRAINT SCORE_PK PRIMARY KEY (SNO);
CREATE SEQUENCE SCORE_SEQ NOCACHE;